spring:
  application:
    name: persona-service-test

  r2dbc:
    url: r2dbc:h2:mem:///persona_db;DB_CLOSE_DELAY=-1
    username: sa
    password:

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

  flyway:
    enabled: false

  security:
    enabled: false